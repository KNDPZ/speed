{
  "name": "crossbench",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local",
    "deploy": "wrangler deploy",
    "test": "node test-worker.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
